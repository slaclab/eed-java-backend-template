name: BuildAndTest
on: 
    push:
        branches:
        - main
concurrency:
  group: {{ '${{' }} github.workflow {{ '}}' }}-{{ '${{' }} github.ref_name {{ '}}' }}
  cancel-in-progress: true
jobs:
    build:
        uses: {{ github_owner }}/{{ project_name }}-deployment/.github/workflows/build-test-create.yml@main
        secrets: inherit

